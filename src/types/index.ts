export type ImageObj = {
  albumId: number;
  id: number;
  thumbnailUrl: string;
  title: string;
  url: string;
};

export type LargeImage = {
  title: string;
  url: string;
};

export type Album = {
  userId: number;
  id: number;
  title: string;
};
